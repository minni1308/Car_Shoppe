export const appliedLoans = (loans) => ({
    type:'APPLIED_LOANS',
    
    loans
    // customerId:customerId,
    // loanAmount:loanAmount,
    // emi:emi,
    // carCost:carCost,
    // selectedFile:selectedFile,
    // time:time,
    // carName:carName,
    // refId:refId
})
